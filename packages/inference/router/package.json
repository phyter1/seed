{
  "name": "@seed/router",
  "version": "1.3.0",
  "description": "Rule-based fleet router backed by mlx-vlm, with deterministic keyword routing and jury mode",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "bun run src/router.ts",
    "router": "bun run src/router.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@seed/core": "workspace:*",
    "@seed/inference-utils": "workspace:*",
    "@seed/jury": "workspace:*",
    "@seed/sensitivity": "workspace:*",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
