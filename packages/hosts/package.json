{
  "name": "@seed/hosts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "discover": "bun run src/discover.ts",
    "run-headless": "bun run src/run-headless.ts"
  },
  "dependencies": {
    "@seed/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
