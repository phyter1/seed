{
  "name": "@seed/hosts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "discover": "bun run src/discover.ts",
    "run-headless": "bun run src/run-headless.ts"
  },
  "dependencies": {
    "@seed/core": "workspace:*",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
