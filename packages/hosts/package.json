{
  "name": "@seed/hosts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "discover": "bun run src/discover.ts",
    "run-headless": "bun run src/run-headless.ts"
  },
  "dependencies": {
    "typescript": "^5.7.0"
  }
}
